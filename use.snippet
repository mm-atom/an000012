	// 获取某天的结束时间
	/**
	 * ${0:变量注释}
	 */
	const r$CURRENT_SECONDS_UNIX = await(() => {
		const p1 = $1;	// 如果为时间戳，则无需使用第二个参数，如为日期字符串，必须使用第二个参数
		// const p2 = '${2|YYYY-MM-DD HH:mm:ss,YYYY-MM-DD,HH:mm:ss|}';	// 时间格式
		// return an12(p1, p2);
		return an12(p1);
	})();